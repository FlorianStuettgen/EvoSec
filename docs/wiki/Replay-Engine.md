# Replay Engine

The dependency-free Python package validates scenarios and normalized JSONL events, evaluates rules, verifies exact expectations, and writes deterministic evidence reports.

Every response is simulation-only. Reports include input hashes, a stable run ID, matched event IDs, correlation metadata, and a PASS/FAIL verdict.
